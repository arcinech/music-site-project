class Home{
  constructor(data) {
    this.data = data;
    console.log(this.data[0]);
  }

  // render(data){
  //   for(song of data){
  //     if(data[song] < )
  //   }
  // }
}

export default Home;
